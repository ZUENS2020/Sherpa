## 背景

说明这次为什么改。

## 目标

说明这次要解决什么问题，验收目标是什么。

## 变更范围

1. 
2. 
3. 

## 验证

1. 本地验证：
2. dev 验证：
3. 工作流结果：

## 环境流转

- [ ] 本 PR 的目标分支是 `dev`
- [ ] 本次改动已经先在 `dev` 验证
- [ ] 本 PR 是 `dev -> main`

说明：

1. 个人分支默认只能先提到 `dev`
2. 只有 `dev` 验证通过后，才允许 `dev -> main`
3. `main` 只接受来自 `dev` 的 PR

## 配置 / 数据影响

- [ ] 不涉及配置变更
- [ ] 涉及环境变量或 Secret
- [ ] 涉及数据库或存储
- [ ] 涉及 K8s / CI / Tunnel / 部署流程

如涉及，请写明：

1. 需要预先配置什么
2. 是否会影响 dev 数据
3. 是否会影响 prod 发布

## 风险

1. 
2. 

## 回滚

1. 
2. 
