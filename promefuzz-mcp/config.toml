# PromeFuzz MCP Configuration

# ======================== Configuration for preprocessor ========================

[preprocessor]

# Run the dummy driver test for api extraction or not.
run_dummydriver_test = false

# Whether to dump the calculated API relevance as a CSV file for human readability.
dump_relevance_as_csv = false

# Whether to dump the library call graph and consumer call graph for debugging.
dump_call_graph = false

# ======================== Configuration for comprehender ========================

[comprehender]

# [MODIFY THIS] The llm used for RAG embedding.
embedding_llm = "embedding_llm"

# The llm used for the comprehension.
comprehension_llm = "minimax"

# The number of document chunks to be retrieved in each RAG query.
retrieve_top_k = 3

# The number of functions to be processed in each batch when comprehending function relevance.
function_batch_size = 24

# ======================== Configuration for binary tools ========================

[bin]

# The path of the preprocessor binary (relative to project root or absolute)
preprocessor = "processor/build/bin/preprocessor"

# The path of the cgprocessor binary
cgprocessor = "processor/build/bin/cgprocessor"

# ======================== Configuration for Large Language Models ========================

[llm]

# [MODIFY THIS] The default llm used for the comprehension and analysis.
default_llm = "minimax"

# Validate the llm or not.
validate_llm = false

# Whether to log each query.
enable_log = true

# ------------- Configuration for LLM (Minimax) -------------

# [MODIFY THIS] Your custom llm name.
[llm.minimax]

# The llm type, currently support "ollama", "openai", "ollama-reasoning", "openai-reasoning", "minimax"
llm_type = "minimax"

# The base url for the minimax api.
base_url = "https://api.minimaxi.com/v1"

# The api key for the minimax api. Use environment variable MINIMAX_API_KEY if empty.
api_key = ""

# The model used for the llm query.
model = "MiniMax-M2.7-highspeed"

# The temperature for the llm query. Recommend to set it lower for generating code.
temperature = 0.5

# The token limit for each llm query. Set it to -1 for the preset limit from model provider.
max_tokens = -1

# The timeout seconds for each query.
timeout = 80

# The maximum retry times for each query.
retry_times = 3

# ------------- Configuration for Cloud LLM (OpenAI) -------------

# [MODIFY THIS] Your custom llm name.
[llm.cloud_llm]

# The llm type, currently support "ollama", "openai", "ollama-reasoning", "openai-reasoning"
llm_type = "openai"

# The base url for the openai api.
base_url = "https://api.openai.com/v1/"

# The api key for the openai api, default using the environment variable "OPENAI_API_KEY".
api_key = ""

# The model used for the llm query.
model = "gpt-4o"

# The temperature for the llm query. Recommend to set it lower for generating code.
temperature = 0.5

# The token limit for each llm query. Set it to -1 for the preset limit from model provider.
max_tokens = -1

# The timeout seconds for each query.
timeout = 80

# The maximum retry times for each query.
retry_times = 3

# ------------- Configuration for LLM 2 (Local LLM) -------------

# [MODIFY THIS] Your custom llm name.
[llm.local_llm]

# The llm type, currently support "ollama", "openai", "ollama-reasoning", "openai-reasoning"
llm_type = "ollama-reasoning"

# The ollama server host.
host = "localhost"

# The ollama server port.
port = 11434

# The model used for the llm query.
model = "deepseek-r1:70b"

# The max tokens for the ollama api.
max_tokens = 6144

# The timeout seconds for each query.
timeout = 600

# The maximum retry times for each query.
retry_times = 3

# ------------- Configuration for LLM 3 (Embedding LLM) -------------

# [MODIFY THIS] Your custom llm name.
[llm.embedding_llm]

# The llm type, currently support "ollama", "openai"
llm_type = "ollama"

# The ollama server host.
host = "localhost"

# The ollama server port.
port = 11434

# The model used for the llm query.
model = "nomic-embed-text"

# The max tokens for the ollama api, set to -1 for the preset limit.
max_tokens = -1

# The timeout seconds for each query.
timeout = 30

# The maximum retry times for each query.
retry_times = 3
